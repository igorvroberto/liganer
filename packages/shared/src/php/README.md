/**
 * Helpers PHP espelhados da API chapas-bobinas.
 * Copiados nos budgets.php dos apps (PHP não tem package import nativo aqui).
 *
 * normalize_situacao($raw) → perdido|analise|ganho
 * list totals from summary.totalKg / summary.total
 * sort by createdAt desc (updates must not reorder)
 */
