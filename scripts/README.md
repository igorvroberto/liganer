# Scripts auxiliares

## Hello-world (Playwright)

Não faz parte das dependências do app. Para rodar localmente:

```bash
npx playwright@1.55.0 install chromium
node scripts/hello-world-demo.mjs
```

Requer `npm run dev` em `http://127.0.0.1:5173/comparador-preco/`.
